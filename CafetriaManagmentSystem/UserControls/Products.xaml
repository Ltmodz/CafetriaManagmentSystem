<UserControl x:Class="CafetriaManagmentSystem.UserControls.Products"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CafetriaManagmentSystem.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="1280" d:DesignWidth="1024">
    <Grid Background="{StaticResource LightGreyBackGround}">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Padding="50,10">
            
            <Grid FlowDirection="RightToLeft">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="5*"/>
                </Grid.ColumnDefinitions>

                <TextBlock FontSize="18" Grid.Column="0" FontWeight="Bold" VerticalAlignment="Center" TextAlignment="Center">
                    بحث المنتجات:
                </TextBlock>
                <TextBox VerticalContentAlignment="Center" FontSize="16" Padding="10,0" Grid.Column="1"></TextBox>
                
            </Grid>
        </Border>

    </Grid>
</UserControl>
